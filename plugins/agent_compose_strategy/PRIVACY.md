# Privacy

This plugin sends configured credentials, Agent node queries, instructions, conversation context, and execution options to the user-configured agent-compose service. Chaitin does not receive this data through the plugin. Data handling and retention are governed by that deployment and its configured model providers. Secrets are used only for request authentication.
