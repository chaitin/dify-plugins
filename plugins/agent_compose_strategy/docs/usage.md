# Configuration and usage

Choose **agent-compose Strategy** in a Dify Agent node. Provide an agent-compose URL and optional bearer token, select a `project/agent`, and configure the sandbox cleanup policy. Connection settings may also be supplied by the plugin runtime environment.
