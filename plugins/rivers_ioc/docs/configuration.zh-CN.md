# 配置与故障排查

插件仅向 `https://intelligence.rivers.chaitin.cn` 发送 `X-CA-Token` 请求头，并设置了明确的
连接与响应超时时间。

- 认证失败：检查令牌及其威胁情报权限。
- 请求限流：稍后重试或检查服务额度。
- 超时或服务不可用：检查百川云服务状态后重试。
- 输入无效：请传入单个 IPv4 或 IPv6 地址，不要传主机名或 CIDR。

插件错误信息不会输出令牌或完整的上游错误响应。
