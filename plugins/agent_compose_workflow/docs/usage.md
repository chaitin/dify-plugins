# Configuration and usage

Set the provider URL to your reachable agent-compose HTTP/Connect endpoint, for example `http://localhost:17410`, and provide a bearer token when authentication is enabled. The default timeout is 900 seconds.

The tool dynamically lists available project agents. Select an agent, pass the user query and optional instruction, then choose whether the sandbox is stopped, retained, or removed after completion.
