# Privacy

This plugin sends configured credentials, user queries, instructions, and execution options to the user-configured agent-compose service. Chaitin does not receive this data through the plugin. Data handling and retention are governed by the selected agent-compose deployment and its configured model providers. Secrets are used only for request authentication.
